import { DoctorDashboard } from "@/components/doctor/doctor-dashboard";

export const metadata = {
  title: "Doctor Control Portal — Medicio",
  description: "Practitioner workspace for credential verification, availability scheduling, hospital affiliations, AI training, and appointment management.",
};

export default function DoctorDashboardPage() {
  return <DoctorDashboard />;
}
